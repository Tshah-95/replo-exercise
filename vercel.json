{
  "builds": [
    {
      "src": "next.config.js",
      "use": "@vercel/next"
    }
  ],
  "installCommand": "npm install",
  "buildCommand": "npm run build && npm run postbuild",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
